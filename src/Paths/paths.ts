import { PathStructure } from "./types";

const paths: PathStructure = {
  root: "/",
  inicio: "/consulta-el-tiempo",
  consultasRealizadas: "/consultas-realizadas",
};

export default paths;
